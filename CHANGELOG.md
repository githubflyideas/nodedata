# nodedata Changelog

## [v5.17.0] — 逐核 CPU；档位写成时长；服务表加父进程

### 逐核：最忙的 3 个核 + 软中断最重的核
整机平均会把单核打满稀释掉：16 核上一个单线程把一个核打到 100%，整机只多 6.25%。
`/proc/stat` 原来只解析 `cpu` 汇总行，现在同一次读取里顺带解析 `cpuN`，不多一次系统调用。

每核一条序列会随核数线性膨胀（128 核就是几百条），所以**历史里只存 5 条，与核数无关**：

| 序列 | 回答什么 |
|---|---|
| `cpu.core_top1/2/3` | 最忙的 3 个核各有多忙。100/12/10 = 一个核被钉死；100/100/98 = 三个线程或三个网卡队列都满了 |
| `cpu.core_softirq_max` | 软中断最重的那个核——网卡中断是不是全压在一个核上 |
| `cpu.busy_pct` | 整机忙碌率，0–100，跟上面同口径 |

`cpu.busy_pct` 是顺带修的一处不一致：v5.16.0 的 USE CPU 行显示 `cpu.user`，那是"各核之和"
（16 核满载 = 1600%），跟 0–100 的"最忙单核"放在一起没法比。现在 CPU 行是
**整机忙碌 51% · 最忙单核 100%**，一眼看出偏斜。`cpu.user/sys/softirq` 在页面上标注"（各核之和）"。

"最忙单核"**无论偏不偏离都显示**：一个被单线程钉死在 100% 的核天天如此，z 永远是 0，
只在偏离时显示的话，最典型的单核瓶颈永远不会出现在页面上。

逐核的最小变化门槛设为 20 个百分点（整机是 3）："N 个核里的最大值"是极值统计，
闲机器上它也在 3% 和 30% 之间来回跳。三条 top 序列归为同一族，一次单核打满不会变成三条线索。

历史里不存"是哪个核"——每轮的第一名可能是不同的核。要看核号，OS 指标页顶部有**实时逐核色带**
（`/api/cores`，只在内存里）：每核一根柱，高度是忙碌率，深色部分是软中断，最忙的那根标红。
不按阈值上色：柱高已经是数值本身。

实测：`taskset -c 1` 绑一个死循环，`/api/cores` 给出 cpu0 2.7%、cpu1 100%，
USE CPU 行 "整机忙碌 51.3% · 最忙单核 100%"，归因给出那个 python3。

### 页面上不再出现 L1–L10
"L6"对人没有意义，"3小时前"才有。偏离表表头、"从多久前开始"列、斜纹说明、线索证据全部改成时长。
名字由后端从 `LagSeconds` 直接算（`deviation.LagName`），不另存一张表——改了档位长度，名字自动跟着变。
接口里的 `lags: ["L1"...]` 保留不动，新增 `lag_names`。

顺带清掉页面上另一套"L"：L0/L3/L4 是**层**的编号，跟 L1–L10 **档位**混在一起，
这正是"不知道 L 是什么意思"的一半原因。现在分别叫"硬阈值检查""变化幅度""线索"。
对比表的列头 `1h/3d` 也统一写成"1小时前/3天前"。

`/api/lagdiag` 原来直接把"σ 未就绪：coarse 层…容差…"这种调试文字显示在页面上。
现在多给一个 `plain` 字段："和 7天前比：历史还不够（已有 5天），暂时判断不了变化算不算异常"。
原来的 `reason` 留给 curl 排障用。

### 服务表加父进程列
是谁把这个服务拉起来的：systemd、containerd-shim、supervisord 是正常托管；
父进程是 bash、sshd、tmux，说明是有人手工起的——SSH 一断、机器一重启，它就没了。

**已消失的服务也保留最后一眼的父进程**（写进事件流，重启后仍在）。这是它最有用的地方：
一个服务不见了，父进程是 bash 的，多半是登录会话结束时跟着收到 SIGHUP，不是被 OOM 或谁 kill 的。

### 修复："1小时前"那一列在重启后头一个小时基本是空的
跟 v5.13.0 的 L1–L5 斜纹格是**同一类错**。对比表的配对容差是 `ago/100`，下限 30 秒，
"1小时前"算出来 36 秒；而重启后原始层还没攒够 1 小时，只能去长期层找，长期层 5 分钟一个点，
离目标时刻最远 150 秒。于是这一列大约四次里有三次是空的。6 小时及以上的列容差本来就 ≥150 秒，不受影响。
下限改为长期层格子的一半。实测：重启后立即看，CPU 组 12 行里 9 行有"1小时前"的值
（剩下 3 行是刚加的逐核序列，确实没有历史）。

## [v5.16.0] — USE 五行：一张会终结的清单；给大模型的文本简报

### 问题：输出是网格，不是判断
49 个指标 × 10 档 = 490 个格子，每一屏都在要求用户自己做综合。
这也是"说不出它像什么"的根源——能被一句话说清的产品，输出有固定形状而且**会终结**。

### USE 五行（`/api/use`，页面顶部）
名字来自 Brendan Gregg 的 USE 方法：对每个资源问三个问题——
Utilization（用得多满）、Saturation（有没有人在排队等它）、Errors（有没有出错）。
它的价值不在多几个指标，而在**逐个资源走一遍，要么找到，要么排除**。

CPU / 内存 / 磁盘 / 网络 / 系统，每行一句话。两条自我约束：

- **不发明阈值。** 状态只由 L0 那 37 项既有判定和 L3 的 z 决定，两者各自已有门槛。
  再加一套"我觉得 80% 算高"只会多一处可错的地方，而且会跟 L0 打架。
  指标只摆"现在多少、平时多少"，判断交给用户。状态词也刻意不含因果：
  说"磁盘 异常"，不说"磁盘是瓶颈"。
- **采不到就说采不到。** 一行"网络 正常"会让人以为网络已经查过了。

基线 = 最近 24h 长期层的中位数，**掐掉最后 10 分钟**——否则一个持续 20 分钟的故障
会把自己的基线抬上去，页面显示"41ms（平时 40ms）"，于是没人觉得不对。
点数不足 12 个时宁可不给基线，也不拿 3 个点编一个。

### 实测发现：子进程的写入被内核记到父进程头上
`bash -c 'dd if=/dev/zero of=... bs=1M count=3000'` 跑完之后，**父 bash 的
`write_bytes` 精确多了 3145805824 字节**。原因是子进程被 `wait()` 回收时，
内核把它的 I/O 累加进父进程的 `signal->ioac`，而 `/proc/PID/io` 读的正是这个。

归因如实转述了内核的口径，但看的人会以为 bash 在写盘。这条已写进"看不到"。
（CPU 不受影响：采的是 utime/stime，不含 cutime/cstime。）

### 给大模型的文本简报（`/api/report`）
排障第一现场越来越多是一个在做初筛的模型。它没有这台机器的任何上下文，
输入按 token 计费，而且**你给它的东西里没提网络，它就会说网络正常**。

`text/plain`，一条 curl 就能贴进对话。六条原则各对应一个实际会犯的错：
只报异常不报全量；纯文本行不用 JSON（`写等待 41ms (平时 2ms)` 约 15 token，
等价 JSON 要 40+）；绝对值配基线不给百分比；归因写在同一行；
**显式声明看不到的东西**；按严重度排序，截断时先丢"正常"那几行。

线索单独成段并标"测试中"——混进资源行里，模型会当成已经成立的判定。

实测这台机器上 399 字符 / 20 行。测试里钉了 1500 字符上界：
超了就说明"只报异常"那条没做到。

### 补 swap 速率
`/proc/vmstat` 原来只取 `pgfault`/`pgmajfault`，现在加 `pswpin`/`pswpout`，
按页换算成字节/秒。SwapFree 是存量，不疼；换入换出的**速率**才疼——
一台机器可以 swap 用掉一半而毫无感觉，也可以只用 2% 但每秒换出 50MB，后者是真卡。

（`unitOf` 里 `swap.in`/`swap.out` 必须排在"`swap.` 开头 = 字节"之前，
否则 50MB/s 会显示成 "50 MiB"，看不出这台机器正在挨打。）

### 已知缺口，写在页面上
- **逐核 CPU**：`/proc/stat` 只解析 `cpu` 汇总行。16 核上单线程打满一个核显示 6.25%。
- **网络归因**：`/proc` 不提供每进程网络计数，要走 netlink socket diag 按 inode 反查。

## [v5.15.0] — 对比表也有了门槛；每组回答"谁干的"

### 对比表绕开了最小变化量闸门
v5.14.0 把门槛加在了 z-score 上，但 `/api/compare` 是**另一条独立路径**，完全没走到。
于是一台睡觉的机器上：

| 指标 | 现在 | 1H前 | 报的变化 |
|---|---|---|---|
| IO 压力 PSI | **0.14%** | 0.01% | ▲1.3k% |
| 最忙盘 util | **0.52%** | 0.08% | ▲550% |
| 写 IOPS | 32.7/s | 0.3/s | ▲10.8k% |
| 运行队列 | 4 | 1 | ▲300% |
| iowait | **0.08%** | 0.00% | ▲新增 |

分母接近 0 时百分比是垃圾。现在每行带 `floor`（复用 `minDeltaFor`），
变化量低于门槛只显示"—"，**原始数值照常显示**——看的人自己判断，我们不替他惊叫。

### 每组回答"谁干的"
看到"写 IOPS 涨了 10.8k%"，下一个问题必然是"**谁写的**"，而对比表原来只有整机数字。

现在每组下面挂一行**同期变化最大的进程**（CPU 组看 `proc.cpu.*`、磁盘看 `proc.io.*`、
内存看 `proc.rss.*`），最多 3 个，只看相对 1 小时前——更长的跨度上进程早换了一批，
拿"7 天前的某个进程"和现在比没有意义。同样有门槛，安静机器不会给出一串名字。

实机验证：起一个持续写盘的进程，磁盘组给出 `sh 629 MB/s`，正是那个写进程。

### 顺带删掉一份死代码
`index.html` 里有两个完全相同的 `cmpCell` 函数，后定义的覆盖前者——
前一份从来没被执行过。

## [v5.14.0] — 最小变化量闸门：统计上显著 ≠ 实际上要紧

一台安静机器的"变化幅度"页上满屏 +6.00。看实际数值就知道问题在哪：

| 指标 | 当前值 | z |
|---|---|---|
| `net.tx` | **202.8 B/s** | +6.00 |
| `net.tx_pps` | **2.8 包/秒** | +6.00 |
| `tcp.retrans` | **0.1 次/秒** | +6.00 |
| `tcp.estab` | **7 个连接** | +4.90 |
| `fs.avail` | 63.9 GiB（**变多了**） | +6.00 |

z 是**尺度无关**的：σ 趋近于 0 时，任何微小变动都是 6σ。

现在每类指标有"值得一提的最小变化量"，低于它的变化按 0 处理（不是"没数据"，所以给 0 不给斜纹）：

| 类型 | 门槛 |
|---|---|
| 网卡吞吐 | 1 MB/s |
| 包速率 / IOPS | 100 /s |
| 内存字节量 | 128 MB |
| 百分比（CPU、内存使用率…） | 3 个百分点 |
| 延迟 | 2 ms |
| 计数（连接数、socket…） | 10 |
| **错误类**（丢包、重传、UDP 溢出） | **1 次/秒**——它们本该是 0，门槛必须低 |

**实测对照**（同一台机器、同一份基线，新旧二进制各跑一遍）：
`|z| ≥ 3` 的指标从 **21 个降到 2 个**。故障语料库 **8/8 仍然通过**——门槛没挡掉真故障。

### 顺带修了一个不真实的测试夹具
`TestLongLagsSurviveRestart` 用 `mem.used` 这个名字，值却只有几百的裸数。
加了 128MB 门槛后它被当成噪声挡掉——**是夹具不真实，不是判定有问题**。
改成真实的 GB 量级。

### 启动内存
读回 14 天历史要解析十几 MB JSON，产生的临时堆 Go 不急着还给系统，RSS 停在虚高水位。
加载后归还一次：**33.9 MB → 31.4 MB**。这不是泄漏，只是没还的解析垃圾——
别人的监控看到"重启后内存 +34%"，报得没错，但那是进程从 5MB 长到稳态的必然过程。

## [v5.13.0] — 终于修掉 L1–L5 斜纹的真正原因：配对容差

**原因不是 σ，是容差。**上一版加的页面自检直接把话说清楚了：

> 配不到 1200s 前的点（容差 30s）：原始层还没攒够这么长，长期层是 5m0s 一格、对不上短档

容差原来是 `H/50`、下限 30 秒。重启之后原始层清空，只剩磁盘上的长期层，
而长期层是 **5 分钟一格**——L3（20 分钟档）的容差只有 30 秒，永远配不上，
于是短档整片斜纹，只能干等原始层自己攒够 20 分钟。

改成 **H/8**：L3 的容差变成 150 秒，正好够上长期层的 5 分钟格子，
**重启后 L3~L10 立即可用**。误差被限制在该档时长的 12.5% 以内——
拿"20 分钟前 ±2.5 分钟"和"20 分钟前"比，对判断变化幅度没有实质影响，
而且 σ 的历史 Δ 用的是同一个容差，口径自洽。
L1/L2 仍需等原始层攒够 5~10 分钟，那是物理下限：要比较"现在和 5 分钟前"，
就必须真有 5 分钟前的数据点。

这个 bug 前后查了五轮，我猜错三次。**最后是靠上一版把自检结论印在页面上才定位的**——
那一行字比我任何一次推断都管用。

### 进程表加父进程列
看到 `Isolated Web Co` 这种名字时，第一个问题是"它是谁的子进程"。
现在每行显示父进程名与 PPID，实测子进程 451/452 正确指向父进程 449。

### 修掉残留文案
结论里还写着"十四档中 X 档超过"——档位早已砍到十档。改为"十个尺度中有 X 个超过"。

## [v5.12.0] — 变化幅度页大幅简化；进程组合计；采集周期 10 秒

### 一页一张表
- 去掉「刚刚发生 / 慢慢变坏 / 原始读数」三个标签，**十个尺度 L1~L10 并排在一张表里**——
  原来把同一件事切成两半看，还要来回切
- 去掉 1h/6h/24h/7d/14d 窗口按钮：这张表看的是"当前值相对各尺度的变化"，窗口只影响取点范围，
  对读表的人没有意义
- 标题下一句话说清楚：L1 = 和 5 分钟前比，L4 = 40 分钟前，L6 = 3 小时前，
  L9 = 昨天这个时候，L10 = 上周这个时候

### OS 指标：重要的排前面，不靠颜色找
把"出现即异常"和"首屏必看"的指标排到每组最前，逐设备细分项沉底，**并去掉颜色标记**——
排序比染色更直接，不用先学一套配色。

### 进程组合计
浏览器、数据库会拉起一堆子进程，单看每个都不大，合起来才是真正的占用。
现在进程表末尾追加**进程组合计行**（沿 ppid 找到最上层非 init 祖先聚合）。

**明细行保留不动**：Chrome/Firefox 的渲染进程按标签页隔离，合并之后就丢掉了
"哪个标签页在吃内存"这个信息，而那恰恰是排查时要的。所以是"明细 + 合计"，不是"合并"。
实测：一个 5 进程的程序，明细 5 行 + 合计 1 行（580 MB），两者都在。

### 采集周期 5 秒 → 10 秒
同时把原始层容量从 17280 点减半到 8640 —— 否则原始层会变成 48 小时，内存白涨一倍。

## [v5.11.0] — 斜纹格自己说明原因；对比表列对齐

### 页面直接告诉你"为什么没有数据"
L1–L4 斜纹这个问题已经来回三次，每次我都得靠猜。根因在于：z 为空只有两个原因——
**σ 没就绪**，或**配不到 t−H 那个点**——而这两件事的修法完全相反，页面上却是同一个斜纹格。

现在"变化幅度"标题下直接写出结论，数据来自 `/api/lagdiag`：

```
L1、L2、L3、L4 现在没有数据（表里显示为斜纹格）：
σ 未就绪：raw 层历史只覆盖 70s，需要 ≥900s 且该小时桶 ≥8 个同时段样本
（还需约 14 分钟；若 history/ 里有历史则重启后立即可用）
```

十个尺度都正常时显示一行绿字。以后看到斜纹格不用再问、也不用 curl。

### 这一页变简单
- 标题「整体偏离度和占用」→ **变化幅度**，副标题「每个指标和"多久之前"比，变了多少」
- 那段 z-score 公式默认**折叠**，标题下只留一句："数字越大 = 变化越超出这个指标平时的波动。
  红 = 变坏，蓝 = 变好，空白 = 没变。" 想看公式点"这个数字怎么算的？"

### 对比表列对齐
"当前"列只有数值，"1H前"等列是"数值 + 变化%"，自动布局下两者宽度不同，表头和数字对不上。
改为固定列宽，数值与变化各占固定宽度。

## [v5.10.0] — 异常现场时间线：一次事故留的是演变，不是瞬间

单个瞬间分不清"尖峰路过"和"持续劣化"。现在一次事故留一条时间线：

```
BEFORE    从内存时序切片（异常前 5 分钟，每 30 秒一点）
ONSET     T0 全量抓
DURING    T+5s / T+15s / T+30s 轻量抓
RECOVERY  结论连续两轮消失后抓一次，或 5 分钟封顶
```

**BEFORE 不抓，是切出来的。**我们内存里压着 24 小时时序，异常发生时直接切"前 5 分钟"——
事后再抓是抓不到已经过去的时刻的。这是相对"出事才开始采"的工具的结构性优势。

### 两条约束都来自"我们自己不能成为性能卡点"

**按类裁剪。** CPU 异常没必要抓五十个磁盘指标。不裁剪的话五次采样就是五倍体积——
上一轮刚把磁盘写入从 21GB/天压到 10MB/天，一次就还回去了。
实测：onset 抓 6 个 /proc 文件，后续每格只抓 3 个；一次事故约 36KB（与单瞬间时代持平）。

**网卡只读白名单。** `/sys/class/net/<if>/` 下有几百个节点，部分读取会触发驱动交互
（ethtool 类节点在故障时可能很慢甚至卡住）——故障机上最容易卡死的，恰恰是最想读的那个文件。
只读 12 个普通 sysfs 属性（operstate / carrier_changes / speed / mtu / statistics 六项），
且只看有 `device` 软链的物理网卡，上限 8 个。

### 其他约束
- 同时进行的采样会话上限 3 个（故障风暴时不能无限开）
- 不为"抓一格"另开定时器：复用 15 秒的 L4 轮次，故障机上不多一个唤醒源
- 一轮跨过多个偏移点时**只抓一次**并记下覆盖了哪几格——同一时刻抓两遍既浪费又误导
  （首次实测就出现了 `during@15s,during@15s`，已修）
- 复用现有限流：同类 5 分钟、同责任方 30 分钟、每小时 6 份

页面口径不变：只说"CPU 异常，现场证据已保存"，不说"是 nginx 导致的"。

## [v5.9.0] — 变了不等于坏了：方向判断 + 绝对值闸门

z 只回答"和平时比变了没有"，不回答"变得好不好"。实测截图里满屏的 Critical 大多是这个缺口造成的。

### 往好的方向变不再告警
**"mem.available 上升 6.0σ（Critical）"——可用内存变多了反而告警**，这是截图里最刺眼的一条。
原因是方向判断只在因果归因那条路径上有，好方向的偏离落到通用路径后没人管。现在两条路径统一。

### 绝对值闸门
变了不等于坏了。一台还剩八成内存的机器，可用内存掉 6σ 也只是缓存在动；
一块 util 3% 的盘，写延迟涨 6σ 仍然是空闲的。现在这些情况不出结论：

| 指标 | 放行条件 |
|---|---|
| 可用内存 / swap | `mem.used_pct ≥ 80` |
| 盘延迟 / IOPS | `disk.util ≥ 30` |
| 根分区可用 | `fs.used_pct ≥ 75` |
| CPU 各态 | 总忙碌度 ≥ 50% |

查不到配套指标时放行（宁可报，不漏）。

**一个例外必须放行**：进程正被 cgroup 配额限流时，它的 CPU 天然上不去、整机忙碌度也不高，
闸门会把它一起挡掉——而"为什么这个服务慢"恰恰就是配额造成的。写测试时踩到了这个，已放行。

### 通用路径同步
`fromDeviations` 现在也应用方向过滤、绝对值闸门、以及 v5.8.0 的"只作证据"名单
（页缓存、吞吐、连接数等本身不代表坏事的指标）。

故障语料库 8/8 仍然通过——闸门没有误挡真故障。

## [v5.8.0] — 同一件事只说一遍；留证不再被噪声占满

### 1. 同族指标只报一条，领头固定
实机注入内存故障时，领头指标在 `mem.available` → `mem.free` → `mem.used_pct` 之间轮换，
**看起来像三件事，其实是一件**。谁的 |z| 大谁上台，标题就跟着跳。

现在同族固定一个代表领头（内存余量用 `mem.available`，因为它已扣掉可回收的页缓存；
盘慢用写延迟，读延迟常年很低），同族其余成员不再逐条罗列，只留在证据里。
族：内存余量 / 盘延迟 / 网卡吞吐 / 丢包 / 根分区 / CPU 压力。
实测复测：三次采样领头都是 `mem.available`，不再轮换。

### 2. "变化本身不代表坏事"的指标不再领头
页缓存被回收（`mem.cached` 下降）是内核在正常干活——内存要用了就回收缓存。
把它当故障就会得出 **"firefox 导致 mem.cached 下降"** 这种现象为真、因果为假的结论，
而且它还会跟真故障抢领头位置（复测时就抢到过一次）。

这类指标（页缓存、buffers、slab、吞吐量、连接数、上下文切换、缺页）降级为**只作证据**。
整类偏离若全是这种，**不出结论**。

### 3. 留证限流
实机注入时四分钟留了 3 份、四个类别轮着来。证据一多，真正的那份就被淹掉，
而且同一责任方的冷却期可能正好把它挡在门外。

- **签名不再带 PID**：崩溃重启循环每次都是新 PID，带上 PID 等于没有去重
- 同类别最小间隔 1 分钟 → **5 分钟**
- 新增**一小时总量上限 6 份**——前两条都是"同类/同责任方"的限流，挡不住"四类轮流报"

## [v5.7.0] — 识别 cgroup 配额限流：被限流的进程是受害者，不是元凶

实机故障注入（stress-ng / fio / cgroup）暴露的问题。四类注入里三类归因正确、45 秒内检出，
唯独 cgroup 限流那次**结论是错的**：把进程关进 0.2 核的 cgroup，nodedata 报
"CPU 劣化 — 责任方 stress-ng（PID 402）"。而 `cpu.stat` 显示它一分钟内被限流 1607 次、
累计 128 秒——**它是受害者，真正的原因是配额**。照那条线索去 kill 进程，方向完全反了。

现在：读 `/proc/PID/cgroup` 找到 cpu 子系统路径，再读 `cpu.stat`（兼容 cgroup v1 的
`throttled_time` 与 v2 的 `throttled_usec`），算出本轮被限流的周期占比、次数与时间比例。
只对快照里的那几十个进程读，不对全部 PID 读。

被限流时结论改口（实测输出）：

```
CPU 劣化：cpu.user 上升 6.0σ — stress-ng-cpu（PID 300）正被 cgroup 配额限流，是受害者
  正被 cgroup 配额限流：100% 的调度周期被掐断（每秒 10 次），被限流掉的时间占 80%。
  它是受害者不是元凶——CPU 高是配额造成的，杀进程解决不了问题，要改配额 /ndtest2
  下一步：cat /sys/fs/cgroup/cpu/ndtest2/cpu.stat · cat .../cpu.cfs_quota_us
```

### 定位表述
README 首段与页面标记统一口径：**nodedata 首先是性能展示工具**。
"线索"区块标为**测试中 · 供参考不作结论**——给出错的推理不如不给推理。

### 实机注入结果（本轮）
| 故障 | 手段 | 检出 | 归因 |
|---|---|---|---|
| CPU | `stress-ng --cpu 1` | ≤45s | ✅ PID 正确 |
| 磁盘 IO | `fio` 持续写 | ≤45s | ✅ PID + 设备 vda |
| 内存 | `stress-ng --vm 1600M` | ≤45s | ✅ PID 正确 |
| cgroup 限流 | 0.2 核配额 | 50s | ❌→✅ 本版修正 |
| 网络 | `tc netem` | — | 沙箱无 sch_netem 模块，未能有效注入 |

全程 nodedata 自身 `self.cpu_pct=0.00`、RSS 19MB、进程扫描 0.9ms。

## [v5.6.0] — 重启后不再盲一小时；"诊断链"降级为"线索"

### L1–L5 长期无数据：第二个原因
v5.5.0 修掉了"基线排除剔空当前小时桶"。但线上仍然复现，这次的线索是**重启后只跑了 31 分钟**。

`MinBaselineSpan = 1 小时` 是 v4.4.0 为了防"刚装上一屏红"加的。代价当时被低估了：
只要 `history/` 里没有可用的长期层（新机器、换了 data-dir、历史被清掉），
**每次重启后画面都要盲一小时**——十个档位全是斜纹，而"刚刚发生"这一屏
恰恰是重启后最该能看的。

现在防"一屏红"靠的是另一道更准的护栏：同时段样本不足 20 的档位不参与 L4 下结论
（L3 照常显示——那一层是摆事实）。所以放宽到 **15 分钟**。

实测（全新数据目录、无任何历史）：L1/L2 在 **18.5 分钟**出现 z（27 个指标），
改之前要等 60 分钟。更长的档位仍需各自的滞后时长攒够，那是物理下限——
要比较"现在和 20 分钟前"，就必须有 20 分钟前的数据点。

若 `history/` 里有历史，重启后几分钟内十个档位全部可用（之前实测 103/107 个指标）。
**所以升级时不要删 data 目录。**

`/api/lagdiag` 的 tier 判断此前没有复现实际的回退条件，会误报成 coarse；
现在与 `RefreshSigma` 完全一致，并给出"还需约几分钟"。

### "诊断链" → "线索"（标记测试中）
nodedata 首先是性能展示工具。关联展示当然更好，但**给出错的推理不如不给**。
该区块改名为**线索**，加"测试中"标记，并明说：以原始数据为准，把这里当成"先看哪儿"的提示。

## [v5.5.0] — 找到并修复 L1–L5 长期无数据的根因

### 根因：基线排除把"当前小时桶"剔空了
v4.3.0 引入的基线排除（指标进入 L4 结论后，异常期样本不参与 σ 估计）有个没想到的后果：
σ 是**按小时分桶**估的，而机器运行不满一天时，"当前小时"只有今天这一份样本，
异常起点通常就在最近一小时内——于是这个桶被全部排除、N=0、该档位变成"未就绪"。

表现完全对得上线上：**L1–L5（用原始层）整片斜纹，L6–L10（用跨天的长期层）正常**，
所有指标的"起始 LAG"都显示 L6。实测一台运行 10 小时的机器，某指标一进 L4 结论，
L1 的 N 从 361 掉到 0；而运行满 24 小时的机器因为有"昨天同一小时"的样本，不受影响——
这解释了为什么这个 bug 这么难复现。

修复：排除后若当前小时桶剩余样本不足，就放弃这一轮排除。
宁可基线被故障污染一轮（尺度偏大），也不能让短档位整片失明——失明是彻底看不见，
污染只是迟钝，而且下一轮就有机会恢复。

### 术语换人话
- 「急性」→ **刚刚发生（5 分钟 ~ 40 分钟）**
- 「劣化」→ **慢慢变坏（1.5 小时 ~ 7 天）**
- 「绝对值」→ **原始读数**
- 「起始 LAG」→ **从多久前开始**
- 「广度」→ **几个时间尺度上都异常**
- 页脚的「L0 巡检 / L1 采集 / L2 转储 / L3 偏离 / L4 诊断」→
  **现在正常吗 → 和以前比变了吗 → 这个变化算异常吗 → 是谁造成的**（悬停有说明）

### 指标按重要性分色
故障时最先看的那几项不该淹在四十行等宽字体里：
- **红色**：出现即异常——丢包、TCP 重传、UDP 缓冲区溢出、D 状态进程、swap 使用、conntrack 水位
- **蓝色**：首屏必看——CPU/负载/可用内存/盘 util 与延迟/PSI/根分区/进程占用
- **灰色**：逐块盘、逐网卡的细分项，默认让位

数字列统一用等宽数字对齐（`tabular-nums`），指标名不再换行。

### 消失的服务保留"遗照"
进程没了之后 CPU/内存就再也查不到。现在消失的服务行会显示：**最后见于何时、当时占多少 CPU/内存、
历史峰值内存**。有人问"你为什么把我的进程杀了"，这几个数就是回答。
（实测：300MB 的进程被杀后，表里保留"最后见于 03:05:55、当时 RSS 310MB、峰值 310MB"。）

## [v5.3.2] — 重启后曲线不再空白；去掉"主视图"这一列

### 重启后关键指标曲线整片空白
曲线取点的容差是"半个步长"（1h 窗口 = 15 秒）。重启后原始层清零，那段时间的数据其实
躺在磁盘上的长期层里，但长期层是 5 分钟一格，拿 15 秒的容差去配必然全空。

现在：**早于原始层起点的时刻改用长期层的容差**（≥2.5 分钟）。
实测（3 天历史 + 启动 35 秒）：1h / 6h / 24h 三个窗口从整片空白变成 **241/241 点满格**。

### 去掉"主视图"列
绝对值页那一列只是内部的"属不属于核心域"标记，对看的人没有任何意义。
换成**窗口内变化**——绝对值页缺的恰恰是"这个数是刚变的还是一直如此"。

## [v5.3.1] — 服务表不再被短命命令占满；新增 /api/lagdiag

### 服务表噪声
"进视野"的条件里有"CPU ≥5% 或 RSS ≥128MB"，于是 `apt-get` 更新软件包、`dash` 执行一条命令
都被收进服务表，几秒后结束变成"已消失"，然后躺满整个保留期。
实测一台桌面机：服务表里 11 行"已消失"全是这种（anacron、apt-get、dash、fwupd、tracker-*）。
表被垃圾占满，真正的服务消失时反而不显眼。

判据是**我们连续看见它多久**（≥3 轮，识别每 60 秒一轮），不是**它自己活了多久**。
后者看着更直接，但选错了，会漏掉两类真实场景：

- **崩溃重启循环**：每两分钟崩一次的服务，自身 uptime 永远不足门槛，会被**永远挡在表外**——
  而崩溃循环恰恰是最该被看见的故障。服务身份是"名字+端口"、跨重启不变，
  按"连续看见"就能正常进表，重启事件也照常记录。
- **无端口、非 systemd 的常驻工作进程**（批处理、数据管道、`nohup` 起的自研程序）：
  按"必须有端口或 unit"会整类漏掉，而它们是真实生产负载。

`apt-get`、`dash` 这类跑几秒的命令最多被看见一轮，自然挡掉。
消失的服务保留期 14 天 → **7 天**。

### 新增 `GET /api/lagdiag`
z 为空只有两个原因：σ 没就绪，或者配不到 t−H 那个点。这两件事的修法完全不同
（前者等历史、后者等原始层攒够 H），但页面上都是同一个斜纹格，看的人无从分辨。

现在一条 curl 就能逐档位说清楚：用的哪一层、该层历史跨度与点数、σ 就绪的指标数、
样例指标当前小时桶的样本数、能配到 t−H 的指标数、最终出 z 的指标数，以及卡在哪一步的说明。

## [v5.3.0] — 重启后短档位不再瞎一小时；SIGTERM 不再丢数据

### L1–L5 重启后立刻可用
L1–L5 只用原始层，而原始层在内存里、**重启即清空**。于是每次重启后短档位要瞎一个小时
（v4.4.0 起基线至少要覆盖一小时才参与判定）——而"突发抖动最先出现在 L1–L4"正是它们的用处，
重启后的第一个小时恰恰最需要看。L6–L10 因为用落盘的长期层，反而立刻就有数。

长期层是 5 分钟一点，而 **L1 的滞后正好是 300 秒**：相邻两个长期层点恰好配成一对，
容差 30 秒也盖得住采样抖动。所以原始层跨度不够时先用长期层顶上，攒够后自动切回原始层
（点更密，尺度估得更准）。实测：重启后 L1/L5 立即就绪（N=36，来自 3 天长期层）；
原始层攒满两小时后切回，N 升到 720 量级。

### SIGTERM 不再丢 down 事件
`systemctl stop` 发的是 SIGTERM，Go 默认直接退出，`defer` 一个都不跑。实测：
杀掉进程后 `services.jsonl` 里**没有 down 事件**。

少了 down 事件，重启后回看这段停机时间，状态机以为"我们一直在看"，
把服务显示成 `—`（确实不在）而不是 `?`（不知道）——**等于告诉运维"你的服务那天挂了"**，
而其实只是我们自己被停了。同时还会丢掉攒着未落盘的长期层点（最多一个 5 分钟周期）。

改为：清理动作集中登记、`sync.Once` 保证幂等，`signal.Notify` 接 SIGINT/SIGTERM，
HTTP 走 `srv.Shutdown`（5 秒上限）。新增回归测试：真起一个进程、发 SIGTERM、断言写出 down。

## [v5.2.0] — 放大图重做 + 磁盘写入降 2000 倍

合并 v5.0.4 / v5.0.5 / v5.1.0 的全部内容，作为一个版本发布。

### 放大图
- 去掉 `preserveAspectRatio="none"`：原来把 800×200 的 viewBox 硬拉到 260px 高，
  坐标轴数字跟着纵向拉伸变形。卡片里的小图同样去掉（26 张全部核对过）。
- 时间轴 5 个刻度（小图仍是两端各一个）；跨度超过 36 小时时带日期，
  否则 7d/14d 窗口上只有时分，读不出是哪天。
- 图下统计条：最新 / 最小 / 最大 / 平均 / 中位数 / P95 / 区间首→末 / 有效点数 / 起止时刻。
- 图下数值表：默认 40 行等间隔抽样（末点必在），带与上一行的变化百分比；
  连续空档折叠成一行——刚装上时 40 行里 38 行是空的，那张表没法看。
- **显示全部点**：切换后逐点列出（不再折叠空档，否则按钮名不副实）。
- **导出 CSV / 复制到剪贴板**：原始 unix 时间戳 + 未格式化的数值，
  直接进 Excel 或 awk；不导 `2.40 KiB/s` 这种人读格式——再解析回数字是自找麻烦。
- 悬停十字线 + 圆点落在曲线上；放大图内可直接切 1h~14d 窗口。
- 合成指标显示全部来源：`CPU 忙碌 %` 原来标题只写 `cpu.user`，
  看的人会以为只统计了用户态；现在是 `cpu.user + cpu.sys + cpu.softirq`。

### 磁盘写入 21 GB/天 → 10 MB/天
实测（读 `/proc/PID/io`）。对一个"不打扰宿主机"的 sidecar，原来的量会直接毁掉可部署性。
三个来源逐个实测定位：

1. **窗口 JSON 转储**：每 30 秒把 5 个窗口各写一遍（稳态约 7.5MB/轮）。
   服务端本就有内存实时构造的兜底（`X-Nodedata-Source: live`），文件对页面并非必需。
   `--dump-interval` 默认改 `0`。→ 361 MB/天
2. **`check.json`**：L0 每周期重写 7.4KB ≈ 128MB/天。
   "内容不变就不写"**无效**——判定文案里嵌着"已用 52.15%""刚启动 324 秒"这类活数字。
   改为根本不写，`/api/check` 与 L4 直接读内存。
3. **L0 写入探针**（最意外）：关掉前两项后逻辑写入只剩 4KB，块层仍有 150MB/天。
   两个对照定位：数据目录搬到 tmpfs——块层没降；量一个 `sleep` 进程——零。
   查到 `F01 临时目录可写` 每 5 秒 create+write+unlink，ext4 上每次分配 inode + 提交日志，
   一天 17280 次。文件系统转只读是持续状态而非瞬时事件，改为 5 分钟探一次。→ **10 MB/天**

长期层历史改为按周期合并写入，但**按各自时间戳分组成多行**——
不能拿第一条的时间戳给整批打标，短档位配对容差只有 30 秒，错标会让这些点永远配不上对。

保留期仍是 14 天，磁盘占用约 9–10 MB（写入量与占用量是两回事）。

## [v5.1.0] — 磁盘写入优化（已并入 v5.2.0）

实测(读 `/proc/PID/io`)：改前稳态 **21 GB/天**，改后 **10 MB/天**，约 2000 倍。
对一个"不打扰宿主机"的 sidecar，原来的量会直接毁掉可部署性。

三个来源，逐个实测定位：

1. **窗口 JSON 转储**（大头）。每 30 秒把 1h/6h/24h/7d/14d 各写一遍，稳态约 7.5MB 一轮。
   但服务端本来就有内存实时构造的兜底路径（响应头 `X-Nodedata-Source: live`），
   这些文件对页面不是必需的。`--dump-interval` 默认改为 `0`（不转储），需要落盘带走时再显式开。
   → 4.2GB/天（冷启动实测）降到 361MB/天。
2. **`check.json`**。L0 每个采集周期把 7.4KB 重写一遍，约 128MB/天。
   先试了"内容不变就不写"——**没用**，判定文案里嵌着"已用 52.15%""刚启动 324 秒"这类活数字，每次都变。
   改为根本不写：结果本来就在 `bcr.latest`，`/api/check` 与 L4 都直接读内存，文件只在开启转储时写。
3. **L0 的写入探针**（这个最意外）。关掉前两项后逻辑写入只剩 4KB，块层却还有 150MB/天。
   两个对照实验定位：把数据目录搬到 tmpfs——块层写入一点没降；量一个 `sleep` 进程——零。
   最后查到 `F01 临时目录可写` 每 5 秒 create+write+unlink 一次，在 ext4 上每次都要分配 inode、提交日志，
   一天 17280 次。文件系统转只读是持续状态不是瞬时事件，改为缓存 5 分钟探一次。
   → 157MB/天降到 **10MB/天**。

另外：长期层历史改为按长期层周期合并写入（新指标出现时会立刻单独落一行，实测本该 5 分钟一行的文件
每周期写了 4~6 行）。合并时**按各自时间戳分组成多行**，不能拿第一条的时间戳给整批打标——
短档位配对容差只有 30 秒，错标会让这些点永远配不上对。

## [v5.0.5] — 合成指标显示全部来源（已并入 v5.2.0）

"CPU 忙碌 %"是 user+sys+softirq 三项合成，放大图标题却只写 `cpu.user`，
看的人会以为只统计了用户态。现在显示 `cpu.user + cpu.sys + cpu.softirq`。

## [v5.0.4] — 放大图重做（已并入 v5.2.0）

之前的放大图只是把小图的 viewBox 用 `preserveAspectRatio="none"` 拉到 260px 高，
坐标轴数字跟着纵向拉伸变形；图下只有一行悬停提示，不把鼠标移上去就是空的。

- **不再拉伸**：去掉 `preserveAspectRatio="none"`，按 800×260 的自身比例渲染，文字是正的。
- **时间轴 5 个刻度**（小图仍是两端各一个）。跨度超过 36 小时时刻度带上日期，
  否则 7d/14d 窗口上只有时分，读不出是哪天。
- **图下统计条**：最新 / 最小 / 最大 / 平均 / 中位数 / P95 / 区间首→末变化 / 有效点数 / 起止时刻。
- **图下数值表**：最多 40 行等间隔抽样，最后一个点必定在表里（那是"现在是多少"），
  每行带与上一行的变化百分比。连续空档折叠成一行"无数据（6 小时，共 39 个采样点）"——
  实例刚装上时 40 行里 38 行是空的，那张表没法看。
- **悬停十字线 + 圆点**落在曲线上，不再只有一行文字。
- **放大图里直接切时间窗口**（1h/6h/24h/7d/14d），不用关掉再回去改。

## [v4.4.0] — 2026-09-12 · 修"刚装上就一屏红"；修五处单位显示错

### 基线太年轻就开始判定（误报根源）
旧规则：一档只要"历史跨度 ≥ 该档时长"就算就绪。L1 = 300 秒，**等于装上 5 分钟就开始出 z**——
那时基线只见过几分钟的安静时段，之后任何正常波动都顶格。
实测一台刚装上的桌面机：十几个指标齐刷刷 ±6.00，广度 2（只有 L1/L2 就绪）。

新增 `MinBaselineSpan = 1 小时`：基线至少覆盖一小时才参与判定，等于"装上一小时后才开始下结论"。

另外 `HighConfN`（同时段样本 < 20）从"仅标记"改为**低置信档位不参与 L4 下结论**；
L3 热力图照常显示——那一层是摆事实，不是下结论。

### 五处单位显示错
| 指标 | 之前 | 现在 |
|---|---|---|
| `fs.avail` | `67046.88M` | `65.5 GiB` |
| `proc.rss.firefox` | `609.58M` | `609.6 MiB` |
| `mem.used_pct` | `14.49 B` ← 当成字节 | `14.49 %` |
| `fs.used_pct` / `conntrack.used_pct` | `32.41` | `32.41 %` |
| `udp.rcvbuf_errors` 等 | `count` | `/s` |

`mem.used_pct` 那个最隐蔽：它命中了"`mem.` 开头 = 字节"这条规则，一个百分数被显示成 `14.49 B`。
现在百分比规则排在最前面，并新增 `unit_test.go` 覆盖 30 个指标 ID。

### 其他
- 页面档位提示还写着"L5–L14（1.5 小时 ~ 28 天）"，档位早已砍到 L10，改为"L5–L10（1.5 小时 ~ 7 天）"。

## [v4.3.1] — 2026-09-12 · 三张表可排序；绝对判定可折叠

### 表格排序
此刻谁在用资源、服务对比、OS 指标对比表都可以点表头排序（L3 偏离度表早已支持）。
- 数值列先降序（先看最大的），文本列先升序，再点反向；空值与"?"永远垫底。
- **数值按原始值排，不按显示文本**：单元格里是 `2.40 KiB/s`、`17.9 MiB`、`1 天 0 小时`
  这类格式化文本，按文本排会得出 "9 MiB > 10 MiB" 这种错误顺序。渲染时写 `data-sort` 原始值。
- **排序在 20 秒自动刷新后保持**。状态按表的 `data-sid` 记住而不是按 DOM 节点——
  表格每次刷新都重建，按节点记的话刷新一次就跳回原序，等于没做。
- 实测：进程表按 CPU（99.8 / 0.2 / 0.2 / 0 / 0）、按 RSS（35MB…4MB）、
  服务表按已运行时长、OS 指标九行按当前值，均严格有序；刷新后保持。

### 绝对判定可折叠
标题栏加"折叠 / 展开"按钮。它固定在页面最上面，机器一切正常时那 41 项占掉整个首屏，
收起来就能直接看到下面的标签页。摘要行（通过 40 / 告警 0 / 失败 1）始终可见，折叠不会藏掉结论。

## [v4.3.0] — 2026-09-12 · 关键指标改曲线；修基线污染；刷新 20 秒

### 关键指标：表 → 曲线
原来"关键指标"和"OS 指标"是同一张表、同样的列，只是少几行——完全重复。
现在关键指标页是 25 条曲线（CPU / 内存 / 磁盘 / 网络 / 套接字 / 进程六组），可选 1h / 6h / 24h / 7d / 14d 窗口，
鼠标悬停读具体时刻的值。表回答"变了多少"，曲线回答"**怎么变的**"——
阶跃（某刻突然换水平）、爬坡（持续上涨，多是泄漏）、周期尖峰（跑批），三种形状对应完全不同的排查方向。

**没有引入 ECharts**：那是约 1 MB 的第三方 JS。走 CDN 则离线/内网机器打开是空白，而"scp 一个文件就能跑"
正是产品前提；vendored 进仓库又违背"只有 Go 和 HTML"。改为内联 SVG 自己画，约 120 行 JS，零依赖。
新增 `GET /api/keyseries?win=6h`；组合指标（CPU 忙碌 % = user+sys+softirq 按核归一）在服务端合成，
因为各条序列采样时刻不一定对齐，客户端逐点相加会错位。

### 修：基线污染——持续几天的故障会自己"痊愈"
σ 每 5 分钟用最近历史重算，历史里**包含正在发生的故障**。尺度取 MAD / 四分位差 / 十分位差的最大者，
一个阶跃故障会把十分位差张到故障幅度那么大。**实测一个持续 3 天的故障，峰值 z 从 4.45 掉到 −1.70**：
机器还病着，报警自己没了。

做法：指标一进入 L4 结论就记下异常起点，重算 σ 时把那之后的样本从 Δ 分布里剔除。
- **上限按比例而不是按时长**：先试过"冻结 2 小时"，3 天的故障里解冻 36 次，照样被污染干净。
  改成 `MaxExcludedFrac = 1/4`——占了历史四分之一以上的状态已经不是异常，是新常态（业务扩容、流量翻倍），该学进去。
- **异常起点向前回溯 1 小时**：我们总是事后才发现故障，标记晚 30 分钟（6 个长期层样本）就让十分位差张开，
  实测 z 从 5.29 掉到 2.00。
- 效果：同一份数据，3 天后 z 从 −1.70 变成 **4.90**，仍然报警。有对照测试。
- 只影响 σ 的样本选取，**不改 z 算法、不动任何页面或接口**；`excludeFrom` 为零值时与旧实现逐位一致（原有对照测试仍通过）。

### 其他
- 自动刷新 5 秒 → **20 秒**（5 秒一轮时不少值还没采到就刷掉了）。
- 修 `/api/keyseries` 曾返回空 body：缺样本处原本写 `NaN`，而 `encoding/json` 不能编码 NaN，
  整个响应编码失败但 HTTP 仍是 200。改为 `*float64`，缺样本输出 `null`。

## [v4.2.0] — 2026-09-12 · 页面重排：L0 置顶 + 四个标签页

原来是一屏长滚动，六段内容按"层"排列（L0/L1/L3/L4），是按实现结构排的，不是按人看的顺序排的。

```
绝对判定 L0              ← 固定在最上面，不进标签页
─────────────────────────
[关键指标] [OS 指标] [整体偏离度和占用] [服务对比]
```

- **L0 置顶**：它是唯一不依赖历史的一层。刚装上的头 40 分钟里 z 全是斜纹、对比表全是"—"，
  只有 L0 能立刻给出判断——新用户第一眼该看到它，而不是一屏"—"。"只看异常项"默认不勾。
- **关键指标**：每类最关键的几项（27 行）+ 诊断链 + 事故留证。一屏之内回答"有没有事、谁干的、下一步敲什么"。
- **OS 指标**：全部采集项（59 行），含逐块盘、逐网卡、PSI、包速率、上下文切换。
- **整体偏离度和占用**：z 热力图 + 此刻谁在用资源。两张表是一次下钻——
  热力图说"disk.await_w 偏了"，紧挨着的进程表说"谁在写、敲什么命令"，拆开就断了。
- **服务对比**：只有服务表。
- **删掉"采集器自检"面板**：列 `collector.procs_scan_ms` 这种，是给开发看的。
  nodedata 自身开销并没有因此看不见——服务表里它是一行，进程表里带"nodedata 自身"标记；
  `/data/health.json` 接口保留，排查时 curl。

切换标签只改显示，数据照常后台刷新，切回来不用重新等。

## [v4.1.0] — 2026-09-12 · L4 结论说服务名；修 fork 风暴下真凶进不了跟踪集合

### L4 责任方带服务名
v4.0.0 识别出了服务，却没把它接进结论。现在整条链通了：

```
CPU 劣化：cpu.user 上升 6.0σ — 责任方 MySQL（mysqld，PID 1200）
  责任方 MySQL (mysqld) PID 1200  占 300% 核…；属于 MySQL（监听 3306）
```

值班的人认的是 MySQL，不是 mysqld，更不是 pid 1200。
`health.txt` 的 `culprit=` 同步改成 `MySQL(mysqld)/1200`。
识别不出服务时退回进程名，不会出现空的"责任方 （PID …）"。

PID 映射按两级：先按 PID 精确匹配，再按进程名兜底——服务识别每分钟一次、L4 每 15 秒一次，
worker 进程的 PID 可能还没进集合，但名字对得上。

### 修：fork 风暴把跟踪集合占满后，真正失控的进程反而进不来
`emitTracked` 原来是"集合满了就拒收新名字"。一次 fork 风暴占满 32 个位置之后，
随后出现的失控进程永远建不了自己的序列，于是 L4 也就永远说不出
"该进程 CPU 相对自身历史 +Nσ"——**最该被抓的那个跑掉了**。

改为淘汰最久未活跃的名字；同时防抖：若被淘汰者本轮仍比新来的更忙则不换。
新增测试复现这个场景（32 个 worker 占满 → 一个吃 150% 核的进程必须挤进来）。

## [v4.0.0] — 2026-09-12 · 转向"服务"；保留期砍到 14 天

方向调整：从"这台机器怎么了"转向"**这台机器上的服务怎么了**"。
值班的人半夜问的从来不是"CPU 多少"，而是"MySQL 是不是又卡了"。

### 减法：保留期 56 天 → 14 天，档位 14 → 10

14 天以前的数据基本不看，56 天保留期是纯浪费。

| | 之前 | 现在 |
|---|---|---|
| 保留期 | 56 天 | **14 天** |
| z 档位 | L1–L14（到 28 天） | **L1–L10（到 7 天）** |
| 长期层内存 | 约 22 MB | **约 5.5 MB** |
| 磁盘 | 37 MB | **约 9 MB** |
| 启动读回 | 1.05 秒 | **约 0.25 秒** |
| 最长窗口 | 30d | **14d** |

z 最长档只到 7 天，是因为**一档需要 2×H 的历史**（既要往回够 H，又要估计这一档平时的波动）。
"跟 14 天前比"由对比表与服务表直接摆出两个时刻的值/在否来回答，不需要统计——
跨两周差一倍，人一眼就知道不正常。

### 新增：服务识别与服务标签页

**全部只读 /proc**：不连服务、不读配置、不执行任何命令（`mysqld --version` 要在生产机上派生进程，不做）。

两层分开：
- **谁值得进视野**（动态，与名字无关）：在监听端口 / 由 systemd 拉起 / 资源占用靠前。
  一台跑着自研服务的机器，我们从没见过它，照样能看见它。
- **它是什么**（查表）：exe basename → java 命令行（ELK 全是 java，靠 exe 分不开）→
  监听端口反查 → 都不认就显示 exe 名。**认不出不影响第一层，绝不丢服务。**

端口反查救的是"编译安装 + 改名部署"：实测把 mysqld 改名叫 `db-main`，exe 认不出来，
但它监听 3306，照样识别成 MySQL。

用 exe 而不是 comm：comm 被内核截断到 15 字节，`postgres: checkpointer` 直接废掉。
主进程取同组最老的那个，nginx worker 重启不算服务重启。

### 新增：服务事件流

存事件不存周期快照——服务集合绝大多数时候一动不动，一台稳定机器 14 天可能就十几条事件、几十 KB，
比指标数据小三个数量级。

出现 / 消失 / 重启，外加 **nodedata 自身的启停**。后者决定了三种状态的区分：

| | 含义 |
|---|---|
| `✓` | 那时在 |
| `—` | 那时确实不在（nodedata 在跑，没看见它） |
| `?` | **不知道**（nodedata 当时没在跑） |

把"不知道"画成"不在"，就等于告诉人"你的 MySQL 那天挂了"，而实际上只是我们没在看。
跟对比表里"没数据显示 — 而不是 0"是同一个原则。

其余约定：启动后 15 分钟是学习期（只建基线，不报"出现"，否则新装机器满屏噪声）；
消失要连续 3 轮未见才确认（一轮抖动不算）；**消失的服务留在表里标灰**——
一个消失的服务如果从表里也消失了，这张表就白做了。

**不进 `health.txt`，不产生告警**：我们分不清"挂了"和"运维手动停的"，分不清就不该报警。
摆事实，看的人两秒钟自己就知道。

### 实机验证
造三个假服务（`redis-server`、`nginx`、改名的 `db-main`）：三条识别路径全部命中，
启动时间与 `ps -o lstart` 精确一致；kill 后 `-` 事件、重启后 `~` 事件均正确，
消失的服务保留在表里。

## [v3.3.0] — 2026-09-12 · 补齐指标覆盖；修两处会毁掉信任的误报

对照 host-dns-perf §4.2 的模块表逐项核对，补上此前缺失的采集。

### 新增采集
- **套接字（整块缺失）**：`/proc/net/sockstat` → socket 总数、TCP inuse/orphan/TIME_WAIT/alloc、UDP inuse；
  `/proc/net/snmp` Tcp: → `tcp.estab`（已建立）、被动新建连接/秒、连接失败/秒。
  TIME_WAIT 堆积与 orphan 上涨是端口耗尽和连接泄漏的早期信号。
- **UDP 错误（关键缺失）**：`udp.rcvbuf_errors`（接收缓冲区溢出）、`udp.in_errors`、`udp.no_ports`、收发包速率。
  对 DNS/NTP/syslog 这类 UDP 服务，应用来不及收包时内核直接丢，**网卡层 rx_drop 一个都不涨**，
  业务侧却已经在超时 —— 只看 `net.rx_drop` 永远看不到。
- **容量**：`fs.used_pct`、`fs.avail`、`fs.inode_used_pct`（statfs，唯一不走 /proc 的采集）；`mem.used_pct`；
  `conntrack.used_pct`。有硬上限的资源，绝对值无法设阈值，占上限的百分比才行。
- `/proc/net/snmp` 改为**按表头名取字段**：列的位置随内核版本变化，旧实现按固定下标会取错；
  表头与数值行不配对时整行跳过而不是错位取值。

### 修两处误报（比漏报更致命）
- **根分区使用率**：L0 用 `(Blocks-Bavail)/Blocks`，把"存在但不可分配"的块算成已用。
  实测一台 `df` 显示 52% 的机器被判成 **96.4%**、直接 fail，`health.txt` 恒为 DOWN。
  改为 `used/(used+avail)`，与 df 同口径；新增与 `df -P` 实测对比的测试。
- **丢包率**：`6 丢 / 77 收 = 7.79%` 也会判 fail。比率要有意义，分母必须够大；
  现在收发包数不足 10000 时跳过判定并说明原因。

### 对比表
新增"套接字"组；关键指标补入内存使用 %、根分区使用 %、收发包速率、UDP 缓冲区溢出、
TCP 已建立 / TIME_WAIT / conntrack 水位。

## [v3.2.0] — 2026-09-12 · 接进现有告警；默认不对外监听

参考 host-dns-perf 的设计文档，补上两处它做对、而 nodedata 一直缺的东西。

### `GET /health.txt` —— 一行文本给监控 grep
`NODEDATA host=… status=NORMAL|WARN|DOWN cpu=… load=… mem_avail=… culprit=名字/PID reason="…" ts=…`

运维不必为 nodedata 学一套 JSON 结构，一条 `curl | grep` 就能接进现有告警。关键字放行首、
主机名同行、异常带 reason，三条都照搬那份文档的约定，因为实践中确实重要。
`culprit=` 是 nodedata 能多给的一项：直接是进程名与 PID，派单时不用再登机器。

自由文本里的 `NODEDATA`、`status=`、换行、引号一律中和。**进程名是攻击者可控的**：
一个叫 `NODEDATA status=NORMAL` 的进程会被写进 `reason=`，让监控侧
`grep 'status=NORMAL'` 在真告警时反而匹配成功，告警就这么被吃掉。有回归测试。

### 默认只监听 127.0.0.1
新增 `--listen`，默认回环。那份文档写得对——这种页面是内网侦察的现成材料，而 nodedata 的页面
比它更敏感（列出进程名与 PID）。要对外必须显式 `--listen 0.0.0.0`，届时启动打印警告。
已实测：回环 200，本机外部地址连接被拒。

## [v3.1.4] — 2026-09-11 · 对比表按"最重要的几项"取舍，并加入进程行

对照同类工具的做法重做首屏对比表：此前是把 31 行指标一股脑铺开，逐块盘逐网卡各占一行，
关键信息被淹没；而且**没有进程行** —— "某进程内存比昨天多了 5 倍"比任何整机指标都直接。

- 分成"关键指标"与"全部指标"两个标签页：前者每类最关键的几项（CPU 4、内存 3、磁盘 4、网络 4）加进程行，
  后者含逐设备、PSI、pps、上下文切换等全部采集项。折叠按钮会让人以为剩下的不重要，标签页是并列关系；
- 新增"进程"组：占用最高的几个进程，各给内存与 CPU 一行，同样带 1h/6h/12h/1d/3d/7d 对比；
- 为此新增 `proc.rss.<名字>` 序列（同名多进程合计）。按"本轮内存占用最高的 8 个"选取而不是绝对阈值：
  小机器上最大的进程可能只有 30MiB，用绝对阈值一个进程行都出不来；同值时按名字排序，避免序列集合每轮抖动。

## [v3.1.3] — 2026-09-11 · 清理：仓库只留 Go 与 HTML

- 删除 `testdata/`（1543 个文件、12MB，其中 `many_procs/` 占 1499 个）。由 `7cc1198` 引入后
  **没有任何 Go 代码引用**，删除后全部测试照常通过。现有测试都用 `t.TempDir()` 现生成 procfs，
  不需要版本库里的固定夹具。
- 删除全部 shell 脚本：`install.sh`（systemd unit 改为写在 README 里，直接贴）、
  `run_strace_test.sh`（引用的 `bin/snapshot-collector` 早已不存在，无法运行）。
- 删除 `internal/store`（ClickHouse）：无人引用，却拖着 16 个第三方依赖，
  并让 `go test ./...` 必须联网。现在 `go.mod` 没有任何 require，整仓库可离线构建与测试。
- 事故留证列表摘要优先给出**进程**责任方（此前取 `Culprits[0]`，IO 结论的第一个是盘、没有 PID，
  页面上只显示 "vda" 而看不到该查哪个进程），设备/接口另置 `place` 字段并在页面一并显示。

## [v3.1.2] — 2026-09-11

- 发布流程：`release.yml` 的构建目标由仓库根目录改为 `./cmd/nodedata`。根包只有 `go:embed` 的页面、没有 main，
  `go build -o nodedata` 产出的是 36KB 归档文件而不是可执行文件，发布包里根本没有程序；
  改后同时产出 amd64/arm64，校验"statically linked"，附 faultlab 与 SHA256SUMS，发布前跑 vet + race + 故障语料库。
  删除与 `release.yml` 内容完全相同的 `main.yml`。
- v3.1.1 没有发布附件，是因为该 tag 指向的提交里还没有 `release.yml`（workflow 是之后才加的），
  打 tag 只触发了 ci。v3.1.2 起 tag 指向的提交已包含修好的 release.yml。
- 一直空闲的盘/网卡不在首屏对比表里单独占行。

## [v3.1.1] — 2026-09-11 · 首屏"关键指标对比"；留证按类别限流

### 首屏：关键指标对比
人话名称 + 当前值 + 1h / 6h / 12h / 1d / 3d / 7d 前同一时刻的值 + 变化百分比，按 CPU / 内存 / 磁盘 / 网络分组；
多块盘、多张网卡时逐设备各一行。历史值服务端精确取（24h 内原始层，更早 5 分钟长期层），
没有历史返回 null、页面显示"—"（不当作 0）；本机没有的指标整行不显示；
红色只标"往坏方向变化 ≥20%"（可用内存往下为坏）；当前与全部历史列都为 0 的逐设备行不显示
（实机上 vdb/vdc/vdd 长期空闲，四行 0 把表撑满）。`GET /api/compare`。
z 热力图与诊断链移到下方，回答"变化是否异常、谁干的"。

### 事故留证
- 限流由"全局 1 分钟"改为"同一类别 1 分钟"。实机注入发现：IO 故障先触发一份 CPU 证据（写入本身吃 CPU），
  全局限流挡掉了紧随其后的 IO 结论，真正的 IO 现场没留下；内存证据被推迟到检查窗口之外；
- 证据里空字段输出 `[]` 而不是 `null`；
- faultlab 核对证据时同时比对类别与 PID（此前只比 PID，把 CPU 类证据误算成 IO 场景通过）。

## [v3.1.0] — 2026-09-11 · 归因、留证、语料库（物理机 / 虚机）

### L1：采集维度与数值修正
磁盘与网络采集此前有多处**数值错误**（不是风格问题）：
- `/proc/diskstats` 字段错位一格：`disk.inflight` 实际是"累计 IO 毫秒"单调计数器；`disk.util` 读的是加权时间，
  且上一轮值从不保存 —— 几乎从不输出；整机 util 各盘相加，两块盘各 60% 就超过上限被丢弃；
- "名字以数字结尾就是分区"：`nvme0n1`、`dm-0`、`md0` 全被跳过，**NVMe 服务器没有任何磁盘指标**；
- `disk.merged` 用本次合并数减上次的 IO 数；
- `net.tx_drop` 实际是**接收包数**（`nums[11%10]`）；
- 只读前 8 个网卡，bond、成员口、VLAN 全部相加，绑定网卡的物理机流量翻倍甚至三倍。

现在：
- 每块整盘出 util / await_r / await_w / 读写 IOPS / 读写吞吐 / 队列（`disk.util@nvme0n1`）；整机汇总不重复计 dm/md，
  整机 util 取最忙那块盘，统一 0–100；
- 每个网卡出 rx / tx / 丢包 / 错包（`net.rx_drop@eth1`）；整机只汇总有 `/sys/class/net/<if>/device` 的网卡；
- 每个进程新增块设备读写（`/proc/PID/io`）、主缺页、状态（D）、1 小时 RSS 增长；
  快照 = CPU 前 10 ∪ 读写前 10 ∪ RSS 增长前 5 ∪ RSS 最大前 5 ∪ D 状态 ∪ 自身；新增 `proc.io.<名字>` 序列。

### L4：因果归因（图 2 那样的结论由 nodedata 自己给出）
- 偏离按症状归成 CPU / IO / 内存 / 网络，每类一条结论：标题、责任方、下一步命令；
- 责任方排序：自身序列在"领头指标偏离的档位"上也偏离 > 在变化开始后才出现 > 此刻用得最多（注明"未必是原因"）。
  必须按档位比较：失控的新进程跑 5 分钟后自己的 L1 就绪且平稳，只看"有无历史"会让归因翻回常年最大的进程；
- 虚机 `cpu.steal` 领头 → 宿主机；`slab` 领头 → 内核；网络只到接口；责任方是自己时明说；
- **结论要求偏离持续 3 个采样点（15 秒）**。约 360 个 z 格下单点 |z|≥3 纯靠噪声几乎每次评估都有一个：
  回放里出现过三次假的"IO 劣化：disk.await_r 3.0σ"。L3 热力图照样显示单点尖峰。

### 事故留证
后台每 15 秒跑 L4（不依赖页面）；带责任方的结论出现时写 `<data-dir>/incidents/<时间>-<类别>.json`：
结论与诊断链、|z|≥2 的指标、进程快照、责任进程线程级 CPU 与内核栈、D 状态进程及其栈、最近 100 条内核日志、
原始 /proc 文件。同一责任方 30 分钟一次，保留 100 份；不采集命令行（防密码泄露）。`/api/incidents`，页面可"立即留证"。

### 故障语料库
- 回放（CI 每次跑）：8 个场景 8/8 通过；安静的一天 5 小时、601 次评估**零误报**；
- `cmd/faultlab`：实验机上真实注入 cpu / io / mem / net 故障，检验类别与 PID，报告耗时；
- `.github/workflows/ci.yml`：vet、race、回放语料库、性能护栏；手动触发时在 runner 上跑 faultlab。

### 已知问题（本版未改）
- **故障持续时基线被污染**：σ 每 5 分钟重算，包含正在发生的故障；稳健尺度用 10–90 分位差，
  故障占到一个小时桶约 10% 以上时短档 σ 被撑大、短档先被掩盖（长档仍能检出，结论不丢）。
  正确做法是结论活跃期间冻结相关指标的基线，并设最长冻结时间以便学习真实的新常态。

## [v3.0.10] — 2026-09-11

### 新增：长期层 —— L9–L14 与 7d/30d 窗口第一次真正可用
此前只有内存里 24 小时的原始点，重启即清空，ClickHouse 代码从未接入：L9–L14（1~28 天）
**永远是斜纹**，7d/30d 按钮实际只有 ≤24h 数据。
- 每 5 分钟从原始点里**抽**一个点（不求平均：抽出的仍是原始点，Δ 与原始数据同分布；
  平均会压小 σ、放大 z），保留 **56 天**，追加写 `<data-dir>/history/YYYY-MM-DD.jsonl`；
  启动时读回。85 条序列 56 天实测：磁盘 37MB，读回 1.05s；内存（含 24h 原始层）约 72MiB；
- 为什么 56 天而不是 28 天：σ 用最近 28 天的同时段 Δ 估计，而 L14 的每个 Δ 要往回够 28 天。
  只留 35 天时 L14 的参照只有 7 天，持续 3 天的劣化占参照 43%，中位数被拖走，
  劣化自己成了"正常"（测试里 L14 z=1.0，漏报）；56 天时同一场景 z=5.8；
- σ 取哪一层按规则而非拍脑袋：配 v(t−H) 的容差 ≥ 长期层步长一半时（L6=3h 起）用长期层，
  否则（L1–L5）用原始层。L6–L8 因此也不再因重启清零；
- 新增 `--history-dir`；
- 新增 `TestLongLagsSurviveRestart`：60 天历史落盘 → 重启只从磁盘装回 → L9–L14 立即就绪、
  正常指标不报、"平稳后最近 3 天每天涨 5%"的指标在 L10–L14 上被抓到；
  `TestHistoryTruncatedLineAndRetention`：崩溃留下的半行跳过、NaN 不落盘、过期日文件删除。

z 的语义提醒：它比的是"这次的 Δ"与"同 lag 的 Δ 平时多大"。一个**一直**匀速上涨的指标
（每天都涨 1%）7 天 Δ 平时就是 +7%，z≈0 —— 长 lag 抓的是"行为变了"，不是"一直在涨"。
容量类问题（磁盘按固定速率写满）归 L0 的绝对判定。

### 移除：`nodedata check` 子命令
L0 在 `serve` 里每个采集周期都跑，结果在首页与 `/api/check`，独立子命令只剩误用
（`install.sh` 装的服务跑的就是它，跑完即退）。`check.PrintResults` 一并删除。

### 部署：一个文件、一行命令
- 页面用 `go:embed` 编进二进制，只需分发 `nodedata`；`--web-root` 仅用于前端开发覆盖；
- README 给出带资源上限的 `systemd-run` 一行命令；`install.sh` 重写为约 40 行，
  写出同样限制的常驻 unit（`CPUQuota=20%`、`MemoryMax=300M`、`GOMAXPROCS=1`、
  `GOMEMLIMIT=200MiB`、`Nice=10`、`IOSchedulingClass=idle`）；
- `--data-dir` 默认 `./data`，不再依赖 web 根目录推断；
- 删除 `setup.sh`（下载 ClickHouse、传 `serve` 根本不认的 `--dsn`）与 `Caddyfile`
  （反代到没人监听的 9701）；README 按现状重写。

### 安全：不再对外暴露当前目录
`/` 原来是 `http.FileServer(http.Dir(webRoot))`：带目录列表、监听 0.0.0.0，
webRoot 找不到 index.html 时回落到**当前目录** —— 在 /root 下启动即可从网络浏览 `/root/.ssh`。
现在 `/` 只返回首页，其他路径 404（`/l0.html` 301 到 `/`）。

## [v3.0.9] — 2026-09-11

### 修复：nodedata 自身吃掉 ~1.5 个核（严重，外部工具抓到 `nodedata-linux-` 150%）
根因是 L3 的 `Series.Lookup` 对 17280 点的缓冲区做**线性扫描**，而 `Build` 对窗口里
每个点、每个就绪 lag 都要 Lookup 一次。成本随缓冲区长度线性上涨，所以启动时正常、
跑满 24 小时后最重。实测（20 个指标、满 24h 缓冲）：

| | v3.0.8 | v3.0.9 |
|---|---|---|
| 一轮转储（5 个窗口） | 34.6 s | 0.08 s |
| σ 全表重算 | 2.7 s | 0.49 s |

转储周期是 30 s，旧实现一轮要 35 s，转储 goroutine 永不空闲（≈1 核）；页面每 5 s 调一次
`/api/diagnosis`，它每次又完整 Build 一个 1h 窗口，叠加后到 1.5 核。
- `Series.Lookup` / `Range` 改二分；拒收时间倒退的点以保证有序前提；
- `/api/diagnosis` 只算每个指标最后一个点（`HeatmapBuilder.Latest`）；
- σ 重算由每指标 14×24=336 遍全量扫描改为每 lag 一遍（双指针配 v(t−H)），
  每个桶只排一次序。结果与旧实现逐位一致（`TestComputeSigmaLagMatchesLegacy`）；
- 采集器路径审计 `openedPaths` 每次读都 append 且从不清理（一天 ~170 万条，
  内存与 GC 扫描随运行时长上涨），改为按 `/proc/<pid>/...` 归一的去重集合；
- 新增 `cmd/nodedata/perf_test.go` 回归护栏：满缓冲一轮转储 > 2 s 即失败
  （把旧 `series.go` 换回去，该测试以 34.6 s 失败）。

### 修复：L3 里找不到对应进程
旧的进程采集每轮从全部 PID 里**随机抽 100 个**，只输出 top-5 的 comm：
- 进程要好几轮才被抽中一次，增量是"距上次被抽中以来"的累计值，首次抽中时 prev=0，
  直接把进程一辈子的 CPU 当 5 秒增量报（`proc.cpu.systemd` 的偏离就是这么来的）；
- 序列时有时无，同时段差分攒不够，z 基本是斜纹；同名多进程同一时刻互相覆盖；
- 按空格切 `/proc/PID/stat`，comm 带空格时 utime/stime 取错字段；
- `kworker/u4:2-events_unbound` 每个变体一条新序列，序列数无界增长；
- 结果里没有 PID，从表格无法走到具体进程 —— nodedata 自己占 150% 也看不到自己。

现在：每轮扫全部 PID（一个 PID 一次 read，1000 进程 < 10 ms）；用 starttime 识别 PID 复用，
首次见到只记基线；以最后一个 `)` 为界解析 comm；内核线程名归一（`kworker/…` → `kworker`）；
CPU 达到 1% 核的名字进入跟踪集合（上限 32），之后每轮都出点（空闲出 0），空闲 1 小时退出；
单位统一为"占一个核的百分比"，与 `cpu.user` 相同。
- 转储 JSON 新增 `procs`：此刻 CPU 前 10 的进程（PID、comm、CPU、RSS），**nodedata 自身永远在列**；
- `health.json` 新增 `self.cpu_pct`、`self.rss_mb`、`collector.procs_scan_ms`；
- 24 小时无新点的序列连同 σ 一起回收（`Series.Prune`）。

### 页面
- L3 表所有列可点击排序（数值列先降序，文本列先升序，z 列按 |z|，空值垫底），
  排序状态在 5 秒自动刷新后保持；
- L3 表下方新增"此刻谁在用 CPU"：PID、进程、CPU、RSS 与下一步命令（点击复制），
  `proc.cpu.<名字>` 行直接标出对应 PID；
- 修正降采样：旧实现从第一个点起步长取点，24h 窗口里表格的"当前值"最多落后 6 分钟，
  现在保证最后一个点是最新点。

### 已知问题（v3.0.10 已修）
- ~~缓冲区只在内存里存 24 小时，L9–L14 永远不会就绪~~
- ~~`install.sh` 装的 systemd 单元跑的是 `nodedata check`~~

## [v3.0.8] — 2026-09-09

### 修复：L3 偏离度表整片顶格（算法错误）

页面上几乎每个指标每一档都是 ±6.00，等于什么都没说。根因是两个：

1. **z 少了位置项。** 公式写的是 `z = Δ / σ`，其中 `Δ = v(t) − v(t−H)`，
   等于假设"Δ 的正常值是 0"。这个假设对任何有稳定漂移的指标都不成立：
   网卡累计字节数、`disk.inflight`、page cache、slab 的 Δ 恒为一个大正数，
   于是 z 永远顶在 +6。现在改成标准化残差
   `z = (Δ − Center) / σ`，Center 是同时段 Δ 的历史中位数，也就是"平时就该涨这么多"。
2. **σ 的下限是编的。** 旧式 `σ = max(1.4826·MAD, 0.02·median|Δ|, N<20 ? 1e-6 : 1/6)`：
   `0.02·median|Δ|` 是 Δ 的量级而不是它的离散程度，代进去 z 恒等于 50；
   `1/6` 这个硬地板让整数型指标（进程 CPU jiffies 在 0/1 之间跳）动一格就恰好是 6.00。
   现在尺度用三个在正态下都收敛到 σ 的稳健估计取最大者
   （1.4826·MAD、四分位差/1.349、十分位差/2.563），完全退化时（速率恒定的计数器、
   恒 0 指标）落到「分辨率」与「量级的 5%」取大者，并统一以分辨率为地板 ——
   整数指标动一格 ⇒ z≈1，动三格 ⇒ z≈3。取三者最大是为了兜住多峰分布
   （周期性抖动、定时任务、锯齿型缓存），MAD 只看中位数附近那一团，会把另一个峰当异常。

### 修复：σ 与 Δ 的配对容差不一致
`ComputeSigma` 里配 `v(t−H)` 传的容差是 `H` 本身 —— 对 L14 来说"28 天内任何一点
都算 28 天前的值"，而 `Z` 用的是固定 30 秒。σ 是松配对的尺度、Δ 是紧配对的量，
两者不是同一个分布，比出来的 z 无意义。现在两边共用
`LagTolerance(H) = clamp(H/50, 30s, 30min)`。

### 变更：样本不够就不出 z
`Ready` 从"历史跨度够"改成"历史跨度够**且**同时段差分样本 ≥ 8"，不够就画斜纹。
另外样本量不足时 z 按 `N/(N+8)` 向 0 收缩（N=8 ×0.5，N=20 ×0.71，N=200 ×0.96），
避免刚启动几分钟就给出满量程结论。

### 新增回归测试
- `internal/deviation/saturation_test.go`：恒速计数器正常时 |z|<1、速率×10 时 z≥4；
  整数指标跳一格 |z|≤2；恒 0 指标 z=0 但跳到 50 要报；样本不足返回 NaN；小样本收缩。
- `cmd/nodedata/heatmap_z_test.go`：13 个"长得像真实节点"的合成指标 + 1 个注入异常，
  跑完整的 Series → RefreshSigma → Build 链路，要求正常指标红格比例 < 2%
  且注入的异常必须被抓到（实测 0.05%，异常抓到 41 格）。

## [v3.0.7] — 2026-09-09

### 修复：进程运行约 5 分钟后必崩（严重）
`internal/deviation.SigmaTable` 内部是裸 map，由 sigmaLoop（5 分钟一次整表重写）
写入，同时被转储循环（30 秒）和 HTTP 处理器读取。Go 的 map 不是并发安全的，
第一次 RefreshSigma 落在读取上就会触发
`fatal error: concurrent map read and map write` —— 这是 runtime 级错误，
recover 不住，进程直接消失。v3.0.5 及更早版本都有这个问题，**必须升级**。
- 改为 `sync.RWMutex` 保护，map 的值改成数组指针，避免每次写入拷贝 14×24 的 σ 数组；
- 索引越界不再 panic，回落到 `LagSigma{Sigma:1e-6}`；
- 新增 `internal/deviation/race_test.go` 与 `cmd/nodedata/heatmap_race_test.go`，
  在 `go test -race` 下把 sigmaLoop 与 Build/DumpAll 并发跑；把锁去掉后这两个
  测试确实会报 DATA RACE，也就是说它们能拦住这个 bug 再次出现。

### 修复：累计型计数器造成的永久性误报
conntrack 丢弃、listen 溢出、OOM 击杀、内核 taint 都是自开机起单调累加/置位的，
按绝对值判定会导致"三个月前抖过一次"永远挂着告警。现在一律只判增量：
- 有人工基线时只对超过基线的新增量告警，没有基线时以本进程首次观测值为起点；
- 计数器回绕/重启（增量为负）自动重新起算；
- taint 是位图，只对基线之后新出现的位告警，已有位仅作说明；
- 累计值照实显示，不再单独构成告警（OOM 历史保留 1 级提示）。

### 新增：人工基线
- `POST /api/baseline` 把当前状态设为标准状态，`GET` 查看，`DELETE` 清除；
- 落盘在 `<data-dir>/baseline.json`，原子写入，重启后自动加载；
- 首页"绝对判定"下方有"把当前状态设为基线 / 清除基线"按钮。

### 修复：`-proc` 对 L0 无效
`-proc` 之前只传给了 L1 采集器，L0 仍在读真 `/proc`。现在 `runServe` 会调用
`check.SetRoots`，`check` 子命令也支持 `-proc / -sys / -data-dir`。

### 变更：首页 L0 不再折叠
41 项判定全部平铺展开，异常项排在前面，保留"只看异常项"开关。

## [v3.0.0-L0] — 2026-09-08

### Overview
L0 Sanity Check release: 39 absolute judgments independent of time-series infrastructure.
First stable layer of v3.0 architecture (L0-L4).

### New Features

#### L0: Sanity Check Subcommand
- `nodedata check [--timeout 5s]` runs 39 absolute judgments in ≤5 seconds
- No ClickHouse, no baselines, direct /proc/sys reads only
- Exit codes: 0=pass, 1=fail, 2=warn

**39 Checks**:
- Time/Sync (3): NTP, accuracy, timezone
- CPU (7): online CPUs, governor, turbo, IRQ balance, preemption, sched, RT
- Memory (8): fragmentation, swappiness, cache, compaction, OOM, zone reclaim, overcommit, dirty pages
- Disk/IO (8): queue depth, scheduler, throttling, IOPS, writeback, trim, RAID, journaling
- Network (6): NICs, MTU, offload, RX/TX buffers
- Filesystem (3): capacity, inodes, mount modes
- Conntrack (2): limit, timeout
- Socket (2): buffer memory, listen queue
- Errors (2): OOM kills, I/O errors

#### L1 Expansion Methods
- `CollectErrorSources()`: dmesg OOM, I/O errors, FS errors, conntrack full, network drops
- `CollectLinkConfig()`: per-NIC speed, duplex, MTU, offload flags
- `CollectSocketStates()`: TCP state breakdown (ESTABLISHED, TIME_WAIT, CLOSE_WAIT, LISTEN)

#### Deviation Layer (L3) Fixes (v0.2.0 sync)
- `ComputeSigmaV2()`: median-based MAD instead of mean baseline
- Proper sigma floor: median(values) / 6 when N ≥ 20
- `ZWithTolerance()`: interval-adaptive tolerance to prevent false positives
- Signed differences in MAD calculation (fixes T_LAG_01)

#### Deployment
- `install.sh v0.2.0`: dependency check, smoke test, systemd integration
- Support for --systemd, --port, --interval, --ch-port parameters
- Go 1.21+ requirement validation

### Fixed

- **T_LAG_01**: Sigma calculation was using absolute diffs instead of signed
  - Result: z-scores were 2x larger than should be
  - Fix: Use signed differences for proper MAD

- **T_LAG_02**: Sigma floor too small (1e-6) for zero-valued metrics
  - Result: z-scores explode when sigma near zero
  - Fix: Use median(values)/6 floor when N≥20

- **Z-score false positives**: Idle systems trigger spikes on noise
  - Result: False alerts on completely normal systems
  - Fix: Z-score tolerance inversely proportional to sampling frequency

### Documentation

- `ARCHITECTURE.md`: 5-layer overview, L0-L1 detail, design invariants
- `DESIGN.md`: zero-allocation strategy, 14-lag rationale, sigma reasoning
- `install.sh`: improved inline comments and error messages

### Technical

**Binaries**:
- `nodedata-check-amd64`, `nodedata-check-arm64`: L0 only (2.6M each)
- `nodedata-linux-amd64`, `nodedata-linux-arm64`: Full v2.x (L0+L1+backend)

**Code Quality**:
- 37 existing acceptance tests still passing (1 SKIP: T_LAG_09)
- Zero-alloc validation: ≤20 allocs per collection round
- Build: CGO_ENABLED=0, pure Go, static binary

### Performance

- L0 check: <1s (target ≤5s)
- Collection: ≤20 allocs/round (v0.2.0 baseline maintained)
- Binary size: 2.6M (amd64)

### Breaking Changes

- L0 now returns exit code 2 for warnings (was not applicable before)
- Sigma computation changed: old baseline calculations no longer apply

### Known Limitations

- L0 error checks require dmesg/journalctl (not available in all containers)
- Socket CLOSE_WAIT attribution still heuristic (needs tcp_diag for certainty)
- Network offload flags default to true (require ethtool -k for accuracy)
- L2 aggregation not yet implemented (planned for v3.1)
- L3 production deployment pending (deviation matrix operational, alerting TBD)

### Upgrade Notes

From v2.x:
- L0 check is opt-in: `nodedata check` does not interfere with existing `nodedata run`
- Deviation layer improvements are backward-compatible but may change alert thresholds
- Consider re-tuning per-metric floors after upgrade

### Contributors

- nodedata core team (v3.0 architecture design)
- v0.2.0 PRD from customer feedback (sigma fixes, deploy clarity)

### Next Steps

- v3.1: L2 aggregation (5-min windows, baseline comparison)
- v3.2: L4 diagnosis chain (64 states → 49 diagnoses)
- v3.3: Production hardening + monitoring dashboard

---

## [v2.x] — Previous

(See git history for v2.0.0 and earlier releases)

